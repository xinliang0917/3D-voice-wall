# 3D 多语种语音墙技术设计文档

## 1. 项目目标

建立一个基于 Three.js / React 的实时语音可视化系统。

核心公式：

```text
Speech
 ↓
ASR
 ↓
Language Detection
 ↓
VoiceMessage
 ↓
3D Text
 ↓
Spatial Flight
 ↓
Landing Effects
 ↓
Voice Wall
```

项目最终需要具备：

**真实语音输入 + 多语种 + 3D Typography + 空间弹射 + 落地特效 + 实时视觉反馈。**

------

# 2. GitHub 技术参考矩阵

| GitHub 项目                  | 本项目借鉴内容                                   | 是否作为基础 |
| ---------------------------- | ------------------------------------------------ | ------------ |
| countertype/three-text       | 多语言 3D 字体、extrusion、glyph cache、CJK、RTL | 是           |
| globe-and-atlas/lexical      | 3D 文本空间、R3F、Bloom、Vignette、场景组织      | 参考         |
| NewKrok/three-particles      | 粒子运动、力场、速度、更新循环                   | 参考         |
| particle-text-3d             | Shader、Bloom、背景粒子、Float32Array、性能优化  | 参考         |
| threejs-text-particle-effect | 文字材质、动态粒子、相机运动                     | 参考         |

其中 `three-text` 是最关键的底层参考，因为它本身定位就是高质量 Web 3D 字体渲染，并且支持 CJK、RTL 和可挤出 Mesh。

------

# 3. 系统架构

```text
                 ┌───────────────┐
                 │   Microphone  │
                 └───────┬───────┘
                         ↓
                 ┌───────────────┐
                 │ Speech Input  │
                 └───────┬───────┘
                         ↓
                 ┌───────────────┐
                 │      ASR      │
                 └───────┬───────┘
                         ↓
                ┌────────────────┐
                │ Language Model │
                └───────┬────────┘
                        ↓
                ┌────────────────┐
                │ SpeechResult   │
                └───────┬────────┘
                        ↓
                ┌────────────────┐
                │  Voice Store   │
                └───┬────────┬───┘
                    │        │
                    ↓        ↓
               Current     Stats
               Speech
                    │
                    ↓
          ┌─────────────────────┐
          │ VoiceText Generator │
          └──────────┬──────────┘
                     ↓
          ┌─────────────────────┐
          │ Spatial Text Layout │
          └──────────┬──────────┘
                     ↓
          ┌─────────────────────┐
          │ Flight Animation    │
          └──────────┬──────────┘
                     ↓
          ┌─────────────────────┐
          │     Voice Wall      │
          └─────────────────────┘
```

------

# 4. 3D 场景设计

## Camera

```text
PerspectiveCamera
FOV 45 ~ 55
```

位置：

```text
(0, 0, 14)
```

目标：

```text
(0, 0, 0)
```

------

# 5. Voice Wall

空间范围：

```text
X = -9 ~ 9
Y = -5 ~ 5
Z = -0.4 ~ 0.3
```

不是单平面，而是一个薄的 3D volume。

这样可以允许文字：

```text
front
middle
back
```

三层甚至多层分布。

------

# 6. Text Rendering Layer

核心：

```text
TextRenderer3D
```

底层优先：

```text
three-text
```

上层包装：

```text
<TextRenderer3D
    text=""
    language=""
    size={}
    position={}
    rotation={}
/>
```

这样以后切换到其他字体库不会改业务层。

------

# 7. VoiceText 生命周期

```text
created
   ↓
spawned
   ↓
flying
   ↓
approaching
   ↓
settling
   ↓
fixed
   ↓
aging
   ↓
removed
```

------

# 8. Flying Animation

初速度：

```text
velocity = direction * speed
```

飞行：

```text
position += velocity * delta
```

靠近墙：

```text
velocity *= damping
```

最终：

```text
spring(damping)
```

进入固定状态。

------

# 9. Spatial Packing

建立：

```text
SpatialPackingEngine
```

输入：

```ts
{
  width,
  height,
  depth
}
```

输出：

```ts
{
  x,
  y,
  z,
  rotation
}
```

算法：

```text
Generate Candidate
       ↓
Bounding Box
       ↓
Collision Detection
       ↓
Score
       ↓
Accept / Reject
```

评分因素：

```text
overlap
distance to wall boundary
distance to existing text
visual balance
```

------

# 10. Collision

第一阶段：

```text
Box3
```

第二阶段优化：

```text
2D/3D spatial grid
```

不要对全部对象执行：

```text
O(n²)
```

当文字数量达到 80～120 时，需要进行空间分区。

推荐：

```text
Spatial Hash Grid
```

------

# 11. 文字分层

```text
Foreground
z = 0.2

Middle
z = 0

Background
z = -0.2
```

Foreground：

```text
brightness 1.0
```

Background：

```text
brightness 0.45
```

------

# 12. Particle Layer

粒子只承担：

```text
background
flight trail
impact
transition
```

不要用粒子取代主要 3D Text。

粒子系统参考 `three-particles` 的 force / gravity / update 结构。

------

# 13. Text Particle Effect

可增加一个：

```text
impactParticleBurst()
```

文字到达墙面以后：

```text
spawn 20~60 particles
```

粒子：

```text
velocity outward
life 300~700ms
opacity 1 → 0
```

形成“文字碰撞墙面”的感觉。

------

# 14. Background Particle

参考 `particle-text-3d`：

```text
300~600 particles
```

通过：

```text
sin()
cos()
```

让背景缓慢漂浮。

该项目还采用了 Bloom、动态 BufferGeometry、Float32Array 直接修改等方式来兼顾视觉和性能，可以作为优化参考。

------

# 15. Post Processing

推荐：

```text
RenderPass
    ↓
Bloom
    ↓
Chromatic Aberration
    ↓
Vignette
```

默认：

```text
Bloom = 0.6
Chromatic = 0.001
Vignette = 0.15
```

不要使用非常强烈的 Glitch。

------

# 16. Interaction

用户：

```text
move mouse
```

相机：

```text
camera.x += mouseX * 0.15
camera.y += mouseY * 0.10
```

产生轻微空间呼吸。

鼠标进入文字：

```text
scale 1.0 → 1.08
z + 0.15
glow +
```

------

# 17. Speech Provider

```text
interface SpeechRecognitionProvider {
    start(): Promise<void>
    stop(): Promise<void>

    onPartialResult(cb)
    onFinalResult(cb)
    onError(cb)
}
```

------

# 18. Mock Provider

Mock Provider 用于：

```text
展览演示
开发
没有 API 时测试
UI 测试
性能测试
```

默认文本：

```text
你好，很高兴认识大家
Hello everyone
こんにちは
안녕하세요
Bonjour tout le monde
Hola a todos
Selamat datang
Xin chào mọi người
```

------

# 19. Language Statistics

Store：

```ts
{
  zh: 12,
  en: 10,
  ja: 8,
  ko: 6,
  fr: 4,
  es: 3
}
```

每个 Final Result：

```text
stats[languageCode]++
```

------

# 20. Real-time UI

中央显示：

```text
LANGUAGE

Chinese · 中文
```

下面：

```text
你好，欢迎来到这里
```

状态：

```text
LISTENING
```

------

# 21. 多语种能力

设计初期直接支持：

```text
zh-CN
en-US
ja-JP
ko-KR
fr-FR
es-ES
de-DE
ar-SA
vi-VN
id-ID
ms-MY
th-TH
```

------

# 22. 性能策略

性能策略参考 `particle-text-3d`：

```text
Float32Array
BufferGeometry
DynamicDrawUsage
Object Reuse
Cached Vectors
Cached Bounding Boxes
```

并限制：

```text
Text = 80 ~ 120
Background Particle = 300 ~ 600
```

------

# 23. React 状态原则

React Zustand 管：

```text
messages
currentRecognition
languageStats
systemStatus
```

Three.js refs 管：

```text
position
rotation
velocity
animation progress
```

禁止：

```text
setState()
```

在每一帧执行。

------

# 24. 文件结构

```text
src/
├── components/
├── three/
├── speech/
├── store/
├── hooks/
├── utils/
└── types/
```

核心：

```text
three/
├── TextRenderer3D.ts
├── VoiceTextObject.ts
├── SpatialPacking.ts
├── CollisionGrid.ts
├── FlightController.ts
├── ParticleEffects.ts
└── ScenePostProcessing.tsx
```

------

# 25. 第一版验收

必须做到：

```text
启动网页
     ↓
看到空 Voice Wall
     ↓
点击 Demo
     ↓
Hello 从前方向墙面飞入
     ↓
停住
     ↓
你好飞入
     ↓
日本語飞入
     ↓
한국어飞入
     ↓
多个文字落到墙面底部并触发落地特效
```

并且：

```text
FPS稳定
文字清晰
中文正常
日文正常
韩文正常
英文正常
```

------

# 26. 第二版验收

加入：

```text
麦克风
↓
实时语音
↓
Partial Result
↓
Final Result
↓
3D Voice Text
```

------

# 27. 第三版验收

接：

```text
WebSocket
↓
真实 ASR
↓
Language Detection
```

前端不需要修改 3D 层。

------

# 28. 最终产品结构

```text
            3D VOICE WALL
                  │
      ┌───────────┼───────────┐
      │           │           │
    Speech      3D Text     Language
      │           │           │
      └───────────┼───────────┘
                  │
            Spatial Memory
                  │
             Voice Wall
```

------

# 29. 最终视觉关键词

整个项目最终应该围绕：

```text
Immersive
Typographic
Spatial
Real-time
Multilingual
Minimal
Futuristic
Elegant
Interactive
```

而不是：

```text
Dashboard
Admin
Traditional Chat
2D Subtitle
Ordinary Word Cloud
```

------

# 30. 设计原则

整个项目只遵守一个核心概念：

> **每一句声音都是一个空间中的 3D 记忆。**

声音出现。

语言被识别。

文字生成。

文字飞行。

文字停留。

下一句话继续加入。

最终：

**所有人的语言，共同组成一面不断生长的 3D Voice Wall。**
